量測variance of histogram(1_VOH_measure.py)：
	1.使用到套件cv2、numpy、os、csv

量測histogram的Chi-square test(2_CHI_measure.py)：
	1.使用到套件cv2、numpy、os、csv

量測Pear correlation coefficients(3_COR_measure.py)：
	1.使用到套件cv2、numpy、os、csv、math、random

量測Global information entropy(4_GIE_meausre.py)：
	1.使用到套件cv2、numpy、os、csv、math

